# UX-ROADMAP-HG-001 — P0.1 implementation authorization

Status: `APPROVED WITH CONDITIONS`  
Authority: Human Gate decision in the current controller thread  
Date: `2026-09-26`

The operational UX roadmap definition artifact
`23b7da3c9836edfaefa2bcf4943ee27f479b2f2a` is approved for bounded
implementation of **P0.1 Reception Arrival / Guided Check-in** only.

The intended workflow is Reception → arrival selection → focused guided
check-in → authoritative refresh → next canonical priority case. Desktop and
mobile must preserve filter, search, selection and queue context. Readiness,
operational blockers, stale conflict and success feedback must be visible.

Backend work is allowed only when a demonstrated P0.1 gap prevents truthful
workflow completion. Housekeeping work is limited to a check-in/readiness
dependency. Reports, Users, other P0 workflows, merge, staging, deploy, main
and production are outside this authorization. Wave 1.2 and its Independent
Critic requirement remain separate.

The P0.1 implementation stops at its Controller checkpoint after targeted
tests, desktop/mobile browser evidence, Pre-Critic, invariant evidence and
orchestration reconciliation. P0.2 is not automatically authorized.
