(page) => (async () => {
  const widths = [375, 390, 430, 768, 1024];
  await page.setExtraHTTPHeaders({ "x-local-access-subject": "subject-a", "x-local-access-email": "a@test.com", "x-hotel-id": "hotel-a" });
  await page.goto("http://127.0.0.1:4174/users"); await page.getByRole("heading", { name: "Users administration" }).waitFor();
  for (const width of widths) { await page.setViewportSize({ width, height: 812 }); await page.waitForTimeout(100); const sw=await page.evaluate(()=>document.documentElement.scrollWidth); if(sw>width) throw new Error(`users overflow ${width}: ${sw}`); await page.getByRole("textbox",{name:"Search users"}).fill("@"); await page.getByRole("button",{name:"View details"}).first().click(); await page.getByRole("dialog").waitFor(); await page.getByRole("button",{name:"Close details"}).click(); await page.getByRole("textbox",{name:"Access subject"}).fill("subject-browser"); await page.getByRole("textbox",{name:"User email"}).fill("browser@example.com"); await page.waitForTimeout(100); await page.getByRole("button",{name:"Create user"}).click(); if(width===375) { await page.getByRole("status").waitFor(); await page.waitForTimeout(100); await page.getByRole("button",{name:"Create user"}).click(); } const duplicate=await page.evaluate(async()=>{const r=await fetch("/api/v1/users",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({access_subject:"subject-browser",email:"browser@example.com",role:"receptionist"})}); return r.status;}); if(duplicate!==409) throw new Error(`create duplicate expected 409 at ${width}, got ${duplicate}`); }
  await page.getByRole("textbox",{name:"Search users"}).fill("subject-browser"); await page.getByRole("button",{name:"View details"}).first().click(); await page.getByRole("dialog").waitFor(); page.once("dialog", dialog => dialog.accept()); await page.getByRole("button",{name:"Deactivate user"}).click(); await page.getByRole("status").waitFor(); if(!(await page.evaluate(() => document.activeElement?.textContent?.includes("View details")))) throw new Error("deactivate focus did not return to opener");
  await page.setExtraHTTPHeaders({ "x-local-access-subject": "subject-hk", "x-local-access-email": "hk@test.com", "x-hotel-id": "hotel-a" }); await page.goto("http://127.0.0.1:4174/users"); await page.waitForTimeout(500); if(!(await page.locator("body").innerText()).includes("No authorized hotel membership")) throw new Error("forbidden UX did not surface backend denial");
  await page.setExtraHTTPHeaders({ "x-local-access-subject": "subject-a", "x-local-access-email": "a@test.com", "x-hotel-id": "hotel-a" });
  await page.setExtraHTTPHeaders({ "x-local-access-subject": "subject-network", "x-local-access-email": "network@test.com" }); await page.goto("http://127.0.0.1:4174/network"); await page.getByRole("heading",{name:"Hotel network"}).waitFor();
  for(const width of widths){ await page.setViewportSize({width,height:812}); await page.waitForTimeout(100); const sw=await page.evaluate(()=>document.documentElement.scrollWidth); if(sw>width) throw new Error(`network overflow ${width}: ${sw}`); await page.getByRole("button",{name:/Hotel A/}).click(); const plan=page.getByRole("combobox",{name:"Property plan"}); await plan.selectOption((await plan.inputValue())==="PRO"?"BASIC":"PRO"); await page.getByRole("status").waitFor(); }
  await page.screenshot({path:"output/playwright/cf-i07-admin.png",fullPage:true}); return widths;
})()
